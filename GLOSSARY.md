# คำศัพท์ Machine Learning ฉบับมือใหม่

คู่มืออธิบายศัพท์ทุกคำที่ใช้ในบทเรียนนี้ ([rul_tutorial.py](rul_tutorial.py),
[rul_lstm.py](rul_lstm.py)) เรียงตามหมวดหมู่ แต่ละคำอธิบายแบบง่ายที่สุด
พร้อมตัวอย่างจากโปรเจกต์จริงเพื่อให้เห็นภาพ

---

## 1) แนวคิดพื้นฐานของ Machine Learning

**Machine Learning (ML)**
การให้คอมพิวเตอร์ "เรียนรู้ pattern จากข้อมูลในอดีต" แล้วเอาไปทำนายสิ่งที่ยังไม่รู้
แทนที่จะเขียนกฎเองทีละข้อ

**Regression**
งานที่ทำนาย "ค่าตัวเลขต่อเนื่อง" เช่น ทำนายว่าเครื่องยนต์เหลืออีกกี่รอบ (ตัวเลขใดๆ
ก็ได้ เช่น 87, 12.5) ตรงข้ามกับ Classification

**Classification**
งานที่ทำนาย "หมวดหมู่" เช่น เสีย/ไม่เสีย, สแปม/ไม่สแปม (บทเรียนนี้ไม่ได้ใช้ แต่ควรรู้จักไว้)

**Feature (ฟีเจอร์)**
ข้อมูลที่ป้อนให้โมเดลใช้ทำนาย เช่น ค่าจากเซ็นเซอร์ต่างๆ (`s_2`, `s_11`, ...)
ในโค้ดเก็บอยู่ในตัวแปร `feature_cols`

**Label / Target**
คำตอบที่อยากให้โมเดลทาย ในบทเรียนนี้คือ `RUL` (Remaining Useful Life)

**Training (การเทรน)**
กระบวนการให้โมเดล "เรียนรู้" จากข้อมูล ทำผ่านคำสั่ง `.fit(X, y)`

**Overfitting**
โมเดล "ท่องจำ" ข้อมูล train มากเกินไป จนทายข้อมูลใหม่ที่ไม่เคยเห็นได้แย่ —
เหมือนนักเรียนท่องข้อสอบเก่ามาตอบ แต่ทำข้อสอบจริงไม่ได้เพราะโจทย์เปลี่ยน

**Data Leakage**
การที่โมเดล (หรือกระบวนการเลือกโมเดล) "แอบเห็น" ข้อมูลที่ไม่ควรเห็น เช่น เอาข้อมูล
test มาช่วยตัดสินใจเลือกโมเดล ทำให้ผลลัพธ์ดูดีเกินจริง

---

## 2) การแบ่งข้อมูล

**Train set**
ข้อมูลที่ใช้ให้โมเดล `.fit()` เรียนรู้

**Validation set**
ข้อมูลที่กันไว้ต่างหาก ใช้ "ตัดสินใจ" เลือกโมเดล/ฟีเจอร์/พารามิเตอร์ — เช็คได้
เรื่อยๆ ไม่จำกัดครั้ง (เหมือนข้อสอบซ้อม) ในโค้ดคือ `val_df` / `val_cut`

**Test set**
ข้อมูลที่แตะแค่ครั้งเดียวตอนจบ หลังตัดสินใจทุกอย่างเสร็จแล้ว ใช้รายงานผลจริง
(เหมือนข้อสอบจริง วันสอบเข้าห้องได้ครั้งเดียว)

**Split ตาม unit / group**
การแบ่ง train/validation โดยกันทั้งเครื่องยนต์ (`unit_number`) ออกไปเลย ไม่ใช่
สุ่มทีละแถว — ป้องกัน data leakage เพราะแถวของเครื่องเดียวกันคล้ายกันมาก

---

## 3) การประเมินผล (Evaluation Metrics)

**RMSE (Root Mean Squared Error)**
"โดยเฉลี่ยโมเดลทายพลาดไปกี่หน่วย" — เอาผลต่างระหว่างค่าทายกับค่าจริงมายกกำลังสอง
เฉลี่ย แล้วถอดราก ยิ่งน้อยยิ่งดี ลงโทษการทายพลาดมากๆ หนักกว่าพลาดน้อยๆ (เพราะยกกำลังสอง)

**MAE (Mean Absolute Error)**
คล้าย RMSE แต่เอาผลต่างมา "ค่าสัมบูรณ์" แล้วเฉลี่ยตรงๆ ไม่ยกกำลังสอง ตีความง่ายกว่า

**Feature Importance**
ตัวเลขบอกว่าโมเดล (เช่น Random Forest) ให้น้ำหนักฟีเจอร์ไหนมากที่สุดในการทำนาย

**Confidence Interval / ช่วงความเชื่อมั่น**
ช่วงตัวเลขที่บอกว่า "ค่าจริงน่าจะอยู่แถวนี้" ยิ่งข้อมูลน้อย ช่วงยิ่งกว้าง (ยิ่งไม่แน่นอน)

---

## 4) โมเดล (Algorithms)

**Linear Regression**
โมเดลง่ายที่สุด พยายามลาก "เส้นตรง" ให้ fit กับข้อมูล ตีความง่าย แต่จับ pattern
ซับซ้อนไม่ได้

**Decision Tree**
โมเดล "ต้นไม้ตัดสินใจ" ต้นเดียว แบ่งข้อมูลเป็นกิ่งไปเรื่อยๆ ตามเงื่อนไข
"มากกว่า/น้อยกว่า" จนถึงคำตอบ

**Random Forest**
เอา Decision Tree หลายร้อยต้นมารวมกัน (แต่ละต้นเห็นข้อมูลคนละส่วน) แล้วเฉลี่ยคำตอบ
— แม่นกว่าต้นไม้เดี่ยว

**Extra Trees**
คล้าย Random Forest แต่สุ่มจุดตัดมากกว่า (ไม่พยายามหาจุดตัดที่ดีที่สุดเป๊ะ) เทรนเร็วกว่า

**Gradient Boosting / HistGradientBoosting**
ปลูกต้นไม้ทีละต้น โดยต้นใหม่คอยแก้ error ที่ต้นก่อนหน้าทำพลาด (ต่างจาก Random
Forest ที่ปลูกทุกต้นพร้อมกัน) มักแม่นกว่า

**Ensemble**
การเอาหลายโมเดลมาเฉลี่ยคำตอบร่วมกัน มักได้ผลดีและเสถียรกว่าโมเดลเดี่ยว เพราะแต่ละ
ตัวผิดคนละแบบ ความผิดพลาดหักล้างกันบางส่วน

**LSTM (Long Short-Term Memory)**
โมเดล deep learning ที่อ่านข้อมูลเป็นลำดับ (time-series) ทีละก้าว พร้อมพก "ความจำ"
ไปด้วย เหมาะกับข้อมูลที่มีลำดับเวลาเช่นเซ็นเซอร์

---

## 5) การเตรียมข้อมูล (Feature Engineering)

**Feature Engineering**
การแปลง/สร้างฟีเจอร์ใหม่จากข้อมูลดิบ เพื่อให้โมเดลเรียนรู้ได้ดีขึ้น — ตามบทเรียน
นี้คือสิ่งที่ช่วยเพิ่มความแม่นยำได้มากกว่าการเปลี่ยนโมเดลเสียอีก

**Window Feature**
ฟีเจอร์ที่คำนวณจาก "ประวัติย้อนหลัง" ไม่ใช่แค่ค่า ณ ปัจจุบัน เช่น ค่าเฉลี่ย 20
รอบล่าสุด

**Rolling Mean / Rolling Std**
ค่าเฉลี่ย / ส่วนเบี่ยงเบนมาตรฐาน ที่คำนวณจากหน้าต่างเลื่อน (เช่น 20 รอบล่าสุด)
กรอง noise ออก เห็นแนวโน้มชัดขึ้น

**Delta**
ค่าปัจจุบัน ลบ ค่าตอนเริ่มต้น — บอกว่าห่างจากจุดเริ่มต้นไปแล้วเท่าไหร่

**Normalization / Scaling**
การปรับสเกลตัวเลขให้อยู่ในช่วงใกล้เคียงกัน (เช่น 0-1) จำเป็นมากสำหรับ neural
network เพราะฟีเจอร์ที่ค่าเลขหลักพันจะกลบฟีเจอร์ที่ค่าเลขหลักหน่วยจนหมด

---

## 6) Hyperparameter & การปรับแต่งโมเดล

**Hyperparameter**
ค่าที่เรา "กำหนดเอง" ก่อนเทรน ไม่ได้เรียนรู้จากข้อมูล เช่น จำนวนต้นไม้
(`n_estimators`), ความลึกของต้นไม้ (`max_depth`), อัตราการเรียนรู้ (`lr`)

**Grid Search**
การตั้งตารางค่าที่จะลองของแต่ละ hyperparameter แล้วลองทุกคู่ผสมที่เป็นไปได้
วัดผลด้วย validation set แล้วเลือกชุดที่ดีที่สุด

**Seed / random_state**
ค่าตั้งต้นของการ "สุ่มเทียม" ในคอมพิวเตอร์ — ล็อกไว้เพื่อให้ผลรันซ้ำได้เหมือนเดิม
ทุกครั้ง (reproducible) การรันหลาย seed แล้วดูค่าเฉลี่ย ± ส่วนเบี่ยงเบน ช่วยบอกว่า
ผลลัพธ์เสถียรแค่ไหน

**Epoch**
1 รอบที่โมเดล (โดยเฉพาะ neural network) ได้เห็นข้อมูล train ครบทุกตัวอย่าง 1 รอบ
เทรนหลาย epoch เพื่อให้เรียนรู้มากขึ้นเรื่อยๆ

**Batch**
ก้อนข้อมูลย่อยที่ป้อนให้โมเดลทีละก้อนระหว่างเทรน (แทนที่จะป้อนทีเดียวทั้งหมด)

**Loss Function**
สูตรวัดว่าโมเดลทายผิดไปเท่าไหร่ระหว่างเทรน (เช่น MSE) ยิ่งน้อยยิ่งดี โมเดล
ปรับตัวเองเพื่อลดค่านี้

**Gradient Descent / Backward Pass**
วิธีที่ neural network ปรับน้ำหนักภายในให้ทายแม่นขึ้น โดยคำนวณว่าน้ำหนักแต่ละตัว
"มีส่วนผิด" แค่ไหน แล้วขยับไปในทางที่ผิดน้อยลง

---

## 7) เครื่องมือ / ไลบรารี

**pandas**
ไลบรารีจัดการข้อมูลแบบตาราง (เหมือน Excel ที่ควบคุมด้วยโค้ด) โครงสร้างหลักคือ
**DataFrame** (ตารางเต็ม) และ **Series** (คอลัมน์เดียว)

**numpy**
ไลบรารีคำนวณเชิงตัวเลข พื้นฐานของแทบทุกไลบรารี ML ในไพทอน

**matplotlib**
ไลบรารีวาดกราฟ

**scikit-learn (sklearn)**
ไลบรารีรวมอัลกอริทึม ML สำเร็จรูป (โมเดลต่างๆ + เครื่องมือวัดผล) ทุกโมเดลใช้สูตร
เดียวกัน: `.fit()` แล้ว `.predict()`

**PyTorch (torch)**
ไลบรารีสำหรับสร้าง neural network (ใช้ในบทเรียน LSTM) ต้องเขียนวงจรการเทรนเอง
ทีละขั้น ต่างจาก sklearn ที่เรียก `.fit()` บรรทัดเดียวจบ

**groupby**
คำสั่ง pandas ที่ "จับกลุ่ม" แถวตามค่าคอลัมน์ เช่น จับกลุ่มตามเครื่องยนต์แต่ละเครื่อง
ก่อนคำนวณอะไรบางอย่างในแต่ละกลุ่ม

**rolling**
คำสั่ง pandas ที่คำนวณค่าจาก "หน้าต่างเลื่อน" ของแถวก่อนหน้า (ใช้คู่กับ groupby
เพื่อไม่ให้ข้อมูลของเครื่องยนต์คนละเครื่องปนกัน)

---

## 8) เครื่องมือรอบข้าง (Environment)

**venv (Virtual Environment)**
Python แยกเฉพาะสำหรับโปรเจกต์นี้ เก็บไลบรารีไม่ให้ปนกับโปรเจกต์อื่น

**pip**
โปรแกรมติดตั้งไลบรารีของ Python (`pip install ...`)

**requirements.txt**
ไฟล์รายชื่อไลบรารี + เวอร์ชันที่ต้องติดตั้ง เพื่อให้รันซ้ำที่ไหนก็ได้ผลเหมือนเดิม

**Git**
โปรแกรมติดตามการเปลี่ยนแปลงของโค้ด (version control) — `commit` คือบันทึกจุด
เปลี่ยนแปลง, `push` คือส่งขึ้น GitHub, `pull` คือดึงของใหม่ลงมา, `clone` คือ
โคลนทั้ง repo มาไว้ในเครื่อง

**GitHub**
เว็บไซต์เก็บโค้ดออนไลน์ (ที่โปรเจกต์นี้อยู่: https://github.com/Aa-newbie/nasa-turbofan-rul-tutorial)

---

## สรุปภาพรวม — คำที่ควรจำขึ้นใจที่สุด 5 คำ

ถ้าจำได้แค่ 5 คำ ให้จำอันนี้:

1. **Train/Validation/Test** — สามชุดข้อมูล คนละหน้าที่ ห้ามปนกัน
2. **RMSE** — ตัวเลขบอกว่าโมเดลทายพลาดไปกี่หน่วยโดยเฉลี่ย ยิ่งน้อยยิ่งดี
3. **Feature Engineering** — การออกแบบข้อมูลป้อนโมเดล มักสำคัญกว่าการเลือกอัลกอริทึม
4. **Overfitting** — โมเดลท่องจำมากไป ทายของใหม่ไม่ได้
5. **Seed** — ค่าตั้งต้นของการสุ่ม ล็อกไว้เพื่อทำผลซ้ำได้
